# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/

# PyInstaller artefacts
*.spec.bak
*.manifest

# Virtual environments
.venv/
venv/
env/

# User data (config, persönliche Seiten — nicht einzuchecken)
data/config.json
# Alle Seiten-Presets ignorieren ...
data/library/pages/*
# ... außer diesen generischen App-Presets (öffentlich, kein personenbez. Inhalt):
!data/library/pages/logic_pro_transport.json
!data/library/pages/logic_pro_files.json

# Lokal generierte Icons (SF Symbols, urheberrechtlich geschützt — nicht einzuchecken)
# Werden beim ersten App-Start automatisch generiert (create_library_icons.py).
assets/icons/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Test artefacts
*.log
scan_result_*.json

# Windows registry export artefacts (project-specific legacy files)
HKEY_CURRENT_USER*/
HKEY_*
Profile Ajazz Software/

# Credentials / secrets
*.env
secrets.py
